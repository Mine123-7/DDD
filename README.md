# Jazz Bar Café - Online Ordering System

ระบบสั่งอาหารออนไลน์สำหรับ Jazz Bar Café พร้อมการเชื่อมต่อฐานข้อมูล MySQL

## 🚀 การติดตั้งและเชื่อมต่อฐานข้อมูล

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. ตั้งค่าฐานข้อมูล

#### 2.1 สร้างฐานข้อมูล
รันไฟล์ `database.sql` ใน MySQL เพื่อสร้างฐานข้อมูลและตาราง:

```bash
mysql -u root -p < database.sql
```

#### 2.2 ตั้งค่าการเชื่อมต่อ
แก้ไขไฟล์ `config.env` ให้ตรงกับการตั้งค่าฐานข้อมูลของคุณ:

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=jazz_bar_cafe
DB_PORT=3306
```

### 3. รันเซิร์ฟเวอร์

```bash
# Development mode (with auto-reload)
npm run dev

# Production mode
npm start
```

เซิร์ฟเวอร์จะรันที่ `http://localhost:3000`

## 📊 โครงสร้างฐานข้อมูล

### ตารางหลัก:
- **categories** - หมวดหมู่อาหารและเครื่องดื่ม
- **menu_items** - รายการเมนู
- **customers** - ข้อมูลลูกค้า
- **orders** - คำสั่งซื้อ
- **order_items** - รายการในคำสั่งซื้อ
- **promotions** - โปรโมชั่นและส่วนลด

## 🔌 API Endpoints

### Health Check
- `GET /api/health` - ตรวจสอบสถานะเซิร์ฟเวอร์และฐานข้อมูล

### เมนู
- `GET /api/menu` - ดูรายการเมนูทั้งหมด
- `GET /api/menu/categories` - ดูหมวดหมู่ทั้งหมด
- `GET /api/menu/popular` - ดูเมนูยอดนิยม
- `GET /api/menu/category/:id` - ดูเมนูตามหมวดหมู่
- `GET /api/menu/search?q=keyword` - ค้นหาเมนู

### คำสั่งซื้อ
- `POST /api/orders` - สร้างคำสั่งซื้อใหม่
- `GET /api/orders` - ดูคำสั่งซื้อทั้งหมด
- `GET /api/orders/:id` - ดูรายละเอียดคำสั่งซื้อ
- `PUT /api/orders/:id/status` - อัปเดตสถานะคำสั่งซื้อ

### ลูกค้า
- `GET /api/customers` - ดูรายชื่อลูกค้าทั้งหมด
- `POST /api/customers` - เพิ่มลูกค้าใหม่
- `GET /api/customers/:id` - ดูข้อมูลลูกค้า
- `PUT /api/customers/:id` - อัปเดตข้อมูลลูกค้า

### โปรโมชั่น
- `GET /api/promotions` - ดูโปรโมชั่นที่ใช้งานได้
- `POST /api/promotions/validate` - ตรวจสอบโค้ดโปรโมชั่น
- `POST /api/promotions` - สร้างโปรโมชั่นใหม่

## 🛠️ การพัฒนา

### โครงสร้างโปรเจค:
```
├── config/
│   └── database.js          # การตั้งค่าฐานข้อมูล
├── routes/
│   ├── menu.js             # API เมนู
│   ├── orders.js           # API คำสั่งซื้อ
│   ├── customers.js        # API ลูกค้า
│   └── promotions.js       # API โปรโมชั่น
├── server.js               # เซิร์ฟเวอร์หลัก
├── package.json            # Dependencies
└── database.sql            # โครงสร้างฐานข้อมูล
```

### การทดสอบการเชื่อมต่อฐานข้อมูล:
เปิดเบราว์เซอร์ไปที่ `http://localhost:3000/api/health`

หากเชื่อมต่อสำเร็จจะเห็น:
```json
{
  "status": "success",
  "message": "Server is running and database is connected",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## 🔧 การแก้ไขปัญหา

### ปัญหาการเชื่อมต่อฐานข้อมูล:
1. ตรวจสอบว่า MySQL กำลังรันอยู่
2. ตรวจสอบ username และ password ใน `config.env`
3. ตรวจสอบว่าฐานข้อมูล `jazz_bar_cafe` ถูกสร้างแล้ว
4. ตรวจสอบสิทธิ์การเข้าถึงฐานข้อมูล

### ปัญหาการรันเซิร์ฟเวอร์:
1. ตรวจสอบว่า port 3000 ไม่ถูกใช้งาน
2. ตรวจสอบ dependencies ถูกติดตั้งครบ
3. ตรวจสอบไฟล์ `config.env` มีอยู่และถูกต้อง

## 📱 การใช้งาน

1. รันเซิร์ฟเวอร์: `npm run dev`
2. เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`
3. ใช้ API endpoints เพื่อจัดการข้อมูล
4. ทดสอบการเชื่อมต่อฐานข้อมูลที่ `/api/health`

## 🎯 คุณสมบัติหลัก

- ✅ การเชื่อมต่อฐานข้อมูล MySQL แบบ Connection Pool
- ✅ API RESTful สำหรับจัดการเมนู, คำสั่งซื้อ, ลูกค้า, และโปรโมชั่น
- ✅ การจัดการ Transaction สำหรับคำสั่งซื้อ
- ✅ การตรวจสอบความถูกต้องของข้อมูล
- ✅ การจัดการ Error และ Response ที่เป็นมาตรฐาน
- ✅ การรองรับการค้นหาและกรองข้อมูล
- ✅ การจัดการสิทธิ์และการตรวจสอบข้อมูล

---

**หมายเหตุ:** อย่าลืมเปลี่ยนข้อมูลการเชื่อมต่อฐานข้อมูลใน `config.env` ให้ตรงกับการตั้งค่าของคุณ
